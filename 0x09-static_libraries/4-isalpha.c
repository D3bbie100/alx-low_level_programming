#include "main.h"

/**
 * _islower - check for lowercase
 * @c: The character to test
 * Return: 1 (Sucess)
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
