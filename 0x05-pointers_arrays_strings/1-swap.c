#include "main.h"

/**
 * swap_int - swaps values of integer variables using pointers
 * @a: pointer to first int variable
 * @b: pointer to second int variable
 * return: nothing
 */

void swap_int(int *a, int *b)
{
	int  c;

	c = *x;
	*x = *y;
	*y = c;
}
