#include "main.h"

/**
 * _abs - returns the absolute value of a number
 * @n: number whose absolute value is ro be returned
 * Return: Always 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

